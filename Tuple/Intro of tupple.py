# tuple is data structure use to store the data like list..
# tuples can store any data type..
# most important tuples are immutable, one tuple is created you can not update..
# data inside tuple.
example=('one','two','three')
# no append ,no insert, no pop, no remove
# why we use tuples?
# = because tuples are faster than list.

# we can use tuple = method,count,index,len function,slicing, min() , max(), sum().