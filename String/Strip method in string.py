name="  Abhijit "
dot="......"
print(name + dot)
print(name.lstrip() + dot)      # To remove the left side space we use lstrip()

print(name.rstrip() + dot)  # To remove the right side space we use rstrip()
